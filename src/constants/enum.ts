export enum BREAKPOINT {
  XS = 490,
  SM = 768,
  MD = 1040,
  LG = 1800,
  XL = 1920,
}

export enum Subscription {
  Monthly = 'monthly',
  Annualy = 'yearly',
}

export enum Plan {
  FREE = 'free',
  CHARGE = 'charge',
}
