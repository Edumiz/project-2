import styled from "styled-components";

export const AddUserStyled = styled.div`
    padding: 20px;

    .return-arrow {
        display: flex;
        margin-bottom: 10px;
        gap: 10px;
    }
    
    .btn {
        display: flex;
        margin-top: 10px;
        gap: 10px;
    }
`