import styled from "styled-components";

export const ViewUserStyled = styled.div`
    padding: 20px;

    .return-arrow {
        display: flex;
        align-items: start;
    }

    .user-name {
        display: flex;
        gap: 10px;
    }
`